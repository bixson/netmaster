apiVersion: v1
kind: Service
metadata:
  name: netmaster
  labels:
    app: netmaster
spec:
  selector:
    app: netmaster
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7681
  type: ClusterIP
